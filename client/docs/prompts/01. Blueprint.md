Generación de Contexto Maestro (Project Blueprint)

Actúa como un Arquitecto de Datos y Tech Lead. Necesito que analices el estado actual de todo este proyecto y generes un archivo de documentación técnica.

Destino del archivo: docs/03-ai-context/01_project_blueprint.md

Por favor, escanea el proyecto y estructura el archivo Markdown exactamente con estas secciones:

1. Estructura de Directorios (Tree):
   Genera un árbol completo del proyecto. Excluye explícitamente directorios irrelevantes como node_modules, .git, y **pycache**. Pon especial énfasis en mapear la carpeta docs/, la nueva estructura del frontend en client/src/modules/ y el pipeline modular en etl_pipeline/.

2. Dependencias y Entorno:
   Extrae las dependencias de Python (si no hay requirements.txt, infiérelas de los imports de los scripts ETL) y de Node.js (del package.json en client).

3. Modelado de Base de Datos y Arquitectura de Datos:
   Analiza los scripts de etl_pipeline y documenta:

Las tablas de la capa raw (Bronce).

Las vistas y vistas materializadas de la capa business (Oro), especificando qué script las crea (ej. el 04 para el Core, el m05 para los Data Marts).

Las funciones RPC creadas en Supabase.

4. Scripts Críticos (ETL Domain-Driven):
   Documenta la secuencia exacta del orquestador 00*run_all_pipeline.py. Resume brevemente qué hace cada script, haciendo la distinción entre los scripts core (01 al 04) y los scripts modulares (m05*...).

5. Estado del Frontend Modular (React/Vite):

Documenta el rol central del archivo src/config/navigation.js.

Lista los módulos activos detectados en src/modules/ (ej. 00-layout, 05-fuerza-laboral).

Mapea qué componentes consumen qué funciones RPC de Supabase.

6. Variables de Entorno (.env):
   Analiza los archivos .env y muestra únicamente las claves (keys), ocultando estrictamente los valores (ej. VITE_SUPABASE_URL=\*\*\*).

Si el archivo 01_project_blueprint.md ya existe, sobrescribe todo su contenido con esta nueva versión para tener el contexto 100% actualizado. Confírmame cuando esté listo.
